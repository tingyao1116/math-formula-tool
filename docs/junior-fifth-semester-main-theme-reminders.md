# 國三上主題整理提醒

## j5-1 單元 1

- `j5-1-1` 先收「連比 / 比例語言」的入口，不要把整個截比例單元都塞進來。
- `j5-1-2` 放平行線截比例線段、三角形截比例與用比例求未知長度。
- `j5-1-3` 放縮放與相似本體：意義、判別、平行造成相似、相似比、直角三角形中的相似。
- `j5-1-4` 保留給相似三角形應用，不要提前把「測樹高、測河寬、影子、海島」全部塞進 `j5-1-3`。
- `可修改版` 直接取 `國三上全重點_易讀版分頁版.md` 的主題頁內容，優先保留原標題與原敘述。
- 數學式保留成前端可渲染格式；來源是 `$...$` 時可直接沿用，避免手改成亂碼。

## j5-3 單元 3

- 這份 `md` 的單元 3 實際主軸是「幾何推理與證明 + 三心」，沒有獨立的「代數證明」頁；不要硬照舊章節碼名稱亂塞。
- `j5-3-1` 改以 `幾何推理與證明的基本想法` 當主題主線，重點是已知、求證、證明、推理步驟。
- `j5-3-2` 放 `證明題的常用工具`，把角和、全等、角平分線、垂直平分線、等腰三角形、輔助線等舊分支集中在這裡。
- `j5-3-3` 放三心本體：`外心與外接圓`、`內心與內切圓`、`重心與中線`、`三心的比較`、`三心的常見應用`。
- 切 PDF 時用頁碼接續前兩章：`21~27` 頁；不要把三心內容切回圓形章。
- `可修改版` 直接取 `國三上全重點_易讀版分頁版.md` 的原主題內容，保留原中文與 `$...$` 數學式，再轉成前端可吃的 `\\(...\\)`。
